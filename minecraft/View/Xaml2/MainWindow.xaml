<Window x:Class="nihilus.View.Xaml2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:nihilus.View.Xaml2"
        mc:Ignorable="d" MinWidth="1000" MinHeight="400"
        Title="Nihilus" Height="720" Width="1200" Style="{StaticResource DefaultWindowV2}">
    <DockPanel Background="{DynamicResource backgroundDefault}">
        <Grid Background="{DynamicResource backgroundDark}">
            <Grid.RowDefinitions>
                <RowDefinition Height="100"/>
                <RowDefinition Height="5*"/>
                <RowDefinition Height="1.6*" MinHeight="120"/>
            </Grid.RowDefinitions>
            <Grid>
                <Image Source="pack://application:,,,/View/Resources/images/paper.png"/>
            </Grid>
            <Grid Grid.Row="1" VerticalAlignment="Top">
                <StackPanel>
                    <TextBlock Text="Server List" Margin="15 10 10 10" Foreground="{DynamicResource textUnselected}" FontSize="20" FontFamily="{StaticResource Montserrat-SemiBold}"/>
                    <ListView Margin="0" Background="Transparent" Style="{StaticResource ServerListViewV2}"
                              ItemContainerStyle="{StaticResource ServerListItemV2}" ItemsSource="{Binding Servers}"
                              SelectedItem="{Binding SelectedServer}">
                    </ListView>
                </StackPanel>
            </Grid>
            <DockPanel Grid.Row="2" LastChildFill="False">
                <Grid  DockPanel.Dock="Bottom"  Background="{DynamicResource backgroundDarker}">
                    <Label Content="v0.5.1" HorizontalAlignment="Center"/>
                </Grid>
                <Grid DockPanel.Dock="Bottom" Margin="10">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0 0 0 10">
                        <Button Margin="0 0 10 0" VerticalAlignment="Bottom">                            
                            <Button.Style>
                                <Style TargetType="{x:Type Button}">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate>
                                                <Border x:Name="imageBorder" Background="{DynamicResource buttonBgrDefault}" CornerRadius="8" Width="45" Height="45">                                                    
                                                    <Image Source="pack://application:,,,/View/Resources/images/Delete.png" Width="21" Height="24" RenderOptions.BitmapScalingMode="Linear"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="imageBorder" Property="Background" Value="{DynamicResource buttonBgrRed}"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Button Margin="0 0 10 0" VerticalAlignment="Bottom">
                            <Button.Style>
                                <Style TargetType="{x:Type Button}">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate>
                                                <Border x:Name="imageBorder" Background="{DynamicResource buttonBgrDefault}" CornerRadius="8" Width="60" Height="60">
                                                    <Image Source="pack://application:,,,/View/Resources/images/Create.png" Width="38.4" Height="24.8" RenderOptions.BitmapScalingMode="Linear" Margin="-1 0 0 0"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="imageBorder" Property="Background" Value="{DynamicResource buttonBgrGreen}"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Button Margin="0 0 10 0" VerticalAlignment="Bottom">
                            <Button.Style>
                                <Style TargetType="{x:Type Button}">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate>
                                                <Border x:Name="imageBorder" Background="{DynamicResource buttonBgrDefault}" CornerRadius="8" Width="45" Height="45">
                                                    <Image Source="pack://application:,,,/View/Resources/images/Import.png" Width="21" Height="24" RenderOptions.BitmapScalingMode="Linear"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="imageBorder" Property="Background" Value="{DynamicResource buttonBgrBlue}"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>
                    </StackPanel>
                </Grid>
            </DockPanel>
        </Grid>
        <Frame Grid.Column="1" DataContext="{Binding SelectedServer, Mode=TwoWay}" Content="{Binding ServerPage}" NavigationUIVisibility="Hidden"></Frame>
    </DockPanel>
</Window>
